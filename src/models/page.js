const { getCollection } = require('@root/db');

/**
 * Pocket Page model
 * @typedef {Object} Page
 * @property {string} owner - Account ID of the owner
 * @property {string[]} posts - List of posts
 */

module.exports = {
  Flag: true,
};
